# HUD Glow-Up (rediseño de UI para enganche) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Elevar el aspecto de Quest Log para maximizar el enganche del usuario tirando de tres palancas — dopamina/recompensa, progresión visible y orgullo/identidad — sin tocar lógica de dominio ni BD.

**Architecture:** Solo capa de presentación. Se añade una función pura (`rankFor`) al dominio con test, dos componentes de UI reutilizables (`AnimatedNumber`, `RadialProgress`), utilidades y keyframes en `globals.css`, y se reescriben visualmente cabecera, fila de tarea, progreso semanal, overlay de nivel, nav y vitrina. Los componentes RSC que necesitan animar pasan a cliente pero siguen recibiendo datos por props desde la página RSC.

**Tech Stack:** Next.js 16 (App Router, RSC + client components), TypeScript, Tailwind CSS 4 (`@theme` tokens en `globals.css`), Vitest (solo dominio).

## Global Constraints

- Todo texto visible por el usuario, en **español**. Código e identificadores en inglés.
- **Colores solo vía tokens** de `globals.css` (`bg-surface`, `text-gold`, `var(--violet)`…). Nada de hex sueltos en componentes. Tokens/keyframes nuevos van en `globals.css`.
- Firma visual HUD intacta: chaflanes `.hud-chamfer`/`.hud-chamfer-sm`, tema oscuro nativo, fuentes Chakra Petch (`font-display`) + Instrument Sans (body).
- **`prefers-reduced-motion: reduce`**: toda animación nueva degrada a estado final instantáneo (o desaparece, en partículas/flotantes). Ampliar el bloque `@media` existente al final de `globals.css`.
- Zonas táctiles ≥ 44px (`min-h-11`/`min-h-14`) intactas. Mobile-first, contenedor `max-w-md`.
- **No** se toca `src/lib/*` de dominio salvo añadir `rankFor` (con test), ni server actions, ni `schema.prisma`, ni migraciones, ni BD.
- Clases de color dinámicas deben ser **strings literales** (Tailwind JIT no ve `text-${x}`): usar records con clases completas.
- Tras cada tarea que compile: `npm run lint` en verde. `npm test` en verde (relevante solo en Task 1). No romper el `export const dynamic = "force-dynamic"` de las páginas.

---

### Task 1: `rankFor` — rango con nombre por nivel (dominio, TDD)

**Files:**
- Modify: `src/lib/gamification.ts` (añadir al final)
- Test: `src/lib/gamification.test.ts` (añadir un `describe`)

**Interfaces:**
- Consumes: nada.
- Produces:
  ```ts
  export type RankAccent = "violet" | "green" | "flame" | "gold";
  export interface Rank { name: string; accent: RankAccent; }
  export function rankFor(level: number): Rank;
  ```
  Bandas (nivel → nombre / accent): 1–4 `Aprendiz`/violet · 5–9 `Guardián`/green · 10–19 `Veterano`/flame · 20–34 `Campeón`/gold · 35+ `Leyenda`/gold.

- [ ] **Step 1: Write the failing test**

Añadir al final de `src/lib/gamification.test.ts`. Primero, añadir `rankFor` a la lista de imports de la línea 2-9 (`import { ... } from "./gamification";`).

```ts
describe("rankFor", () => {
  it("asigna nombre y acento por banda de nivel", () => {
    expect(rankFor(1)).toEqual({ name: "Aprendiz", accent: "violet" });
    expect(rankFor(4)).toEqual({ name: "Aprendiz", accent: "violet" });
    expect(rankFor(5)).toEqual({ name: "Guardián", accent: "green" });
    expect(rankFor(9)).toEqual({ name: "Guardián", accent: "green" });
    expect(rankFor(10)).toEqual({ name: "Veterano", accent: "flame" });
    expect(rankFor(20)).toEqual({ name: "Campeón", accent: "gold" });
    expect(rankFor(35)).toEqual({ name: "Leyenda", accent: "gold" });
  });

  it("nunca falla por debajo de 1 (defensa)", () => {
    expect(rankFor(0).name).toBe("Aprendiz");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- gamification`
Expected: FAIL — `rankFor is not a function` / no exportado.

- [ ] **Step 3: Write minimal implementation**

Añadir al final de `src/lib/gamification.ts`:

```ts
export type RankAccent = "violet" | "green" | "flame" | "gold";
export interface Rank {
  name: string;
  accent: RankAccent;
}

// Rango con nombre por nivel: identidad del jugador. Se deriva del nivel,
// nunca se almacena. Ascendente; el nivel más alto reutiliza el oro (prestigio).
const RANK_BANDS: { min: number; name: string; accent: RankAccent }[] = [
  { min: 35, name: "Leyenda", accent: "gold" },
  { min: 20, name: "Campeón", accent: "gold" },
  { min: 10, name: "Veterano", accent: "flame" },
  { min: 5, name: "Guardián", accent: "green" },
  { min: 1, name: "Aprendiz", accent: "violet" },
];

export function rankFor(level: number): Rank {
  const band = RANK_BANDS.find((b) => level >= b.min) ?? RANK_BANDS[RANK_BANDS.length - 1];
  return { name: band.name, accent: band.accent };
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm test -- gamification`
Expected: PASS (todos los `describe`, incluido `rankFor`).

- [ ] **Step 5: Commit**

```bash
git add src/lib/gamification.ts src/lib/gamification.test.ts
git commit -m "feat: rangos con nombre por nivel (rankFor, puro + test)"
```

---

### Task 2: `AnimatedNumber` + keyframes base

**Files:**
- Create: `src/components/ui/AnimatedNumber.tsx`
- Modify: `src/app/globals.css` (keyframe `num-bump` + entrada en el bloque reduce-motion)

**Interfaces:**
- Consumes: nada.
- Produces: `<AnimatedNumber value={number} className?={string} duration?={number} />` — componente cliente. Cuenta de 0 → `value` al montar; hace "bump" y recuenta cuando `value` cambia entre renders. Con reduce-motion muestra el valor final sin animar.

- [ ] **Step 1: Crear el componente**

Create `src/components/ui/AnimatedNumber.tsx`:

```tsx
"use client";

import { useEffect, useRef, useState } from "react";

// Contador con cuenta ascendente (dopamina) y "bump" al cambiar de valor.
// Arranca en 0 al montar para que el número "suba" al abrir la pantalla.
// Respeta prefers-reduced-motion: salta al valor final sin animar.
export function AnimatedNumber({
  value,
  className = "",
  duration = 600,
}: {
  value: number;
  className?: string;
  duration?: number;
}) {
  const [display, setDisplay] = useState(0);
  const [bump, setBump] = useState(false);
  const prev = useRef(0);
  const raf = useRef<number | undefined>(undefined);

  useEffect(() => {
    const reduce = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    const from = prev.current;
    const to = value;
    prev.current = value;

    if (reduce || from === to) {
      setDisplay(to);
      return;
    }

    setBump(true);
    const start = performance.now();
    const tick = (now: number) => {
      const t = Math.min(1, (now - start) / duration);
      const eased = 1 - Math.pow(1 - t, 3); // easeOutCubic
      setDisplay(Math.round(from + (to - from) * eased));
      if (t < 1) {
        raf.current = requestAnimationFrame(tick);
      } else {
        setBump(false);
      }
    };
    raf.current = requestAnimationFrame(tick);

    return () => {
      if (raf.current) cancelAnimationFrame(raf.current);
    };
  }, [value, duration]);

  return <span className={`${bump ? "num-bump" : ""} ${className}`}>{display}</span>;
}
```

- [ ] **Step 2: Añadir el keyframe en `globals.css`**

Insertar tras el bloque `.rise-in` (después de la línea `}` de `.rise-in`, ~línea 102):

```css
/* Contador que "salta" al cambiar de valor. */
@keyframes num-bump {
  0% { transform: scale(1); }
  40% { transform: scale(1.18); }
  100% { transform: scale(1); }
}
.num-bump {
  display: inline-block;
  animation: num-bump 0.35s ease-out;
}
```

- [ ] **Step 3: Añadir `num-bump` al bloque reduce-motion**

En el `@media (prefers-reduced-motion: reduce)` del final, añadir `.num-bump` a la lista de selectores con `animation: none;`:

```css
@media (prefers-reduced-motion: reduce) {
  .xp-shine, .rise-in, .check-particle, .celebrate-toast, .levelup-badge, .num-bump {
    animation: none;
  }
  .check-particle { display: none; }
}
```

- [ ] **Step 4: Verificar compilación y lint**

Run: `npm run lint`
Expected: sin errores.

- [ ] **Step 5: Commit**

```bash
git add src/components/ui/AnimatedNumber.tsx src/app/globals.css
git commit -m "feat: componente AnimatedNumber (count-up + bump) y keyframe"
```

---

### Task 3: Cabecera → "carné de aventurero"

**Files:**
- Modify: `src/components/dashboard/PlayerHeader.tsx` (reescritura del componente; se vuelve cliente)

**Interfaces:**
- Consumes: `rankFor` (Task 1), `<AnimatedNumber>` (Task 2), `levelProgress` (ya existe), `ProgressBar` (ya existe).
- Produces: mismo contrato de props que hoy (`name, xp, coins, streak, lostStreak`). La página `src/app/page.tsx` no cambia.

- [ ] **Step 1: Reescribir el componente**

Replace todo `src/components/dashboard/PlayerHeader.tsx` con:

```tsx
"use client";

import { levelProgress, rankFor, type RankAccent } from "@/lib/gamification";
import { ProgressBar } from "@/components/ui/ProgressBar";
import { AnimatedNumber } from "@/components/ui/AnimatedNumber";

// Clases literales por acento (Tailwind JIT no ve clases dinámicas).
const ACCENT: Record<RankAccent, { text: string; chip: string }> = {
  violet: { text: "text-violet", chip: "bg-violet-soft text-violet" },
  green: { text: "text-green", chip: "bg-green-soft text-green" },
  flame: { text: "text-flame", chip: "bg-flame-soft text-flame" },
  gold: { text: "text-gold", chip: "bg-gold-soft text-gold" },
};

export function PlayerHeader({
  name,
  xp,
  coins,
  streak,
  lostStreak,
}: {
  name: string;
  xp: number;
  coins: number;
  streak: number;
  lostStreak: number;
}) {
  const p = levelProgress(xp);
  const rank = rankFor(p.level);
  const accent = ACCENT[rank.accent];
  const broken = streak === 0;

  return (
    <header className="hud-chamfer hud-panel rise-in border border-edge p-4">
      <div className="flex items-center justify-between">
        <div>
          <p className="text-sm text-muted">{name}</p>
          <p className="font-display text-3xl font-bold leading-tight">
            Nivel <span className={accent.text}>{p.level}</span>
          </p>
          <p className={`mt-0.5 font-display text-xs font-semibold uppercase tracking-[0.18em] ${accent.text}`}>
            {rank.name}
          </p>
        </div>
        <div className="flex items-center gap-2">
          <div
            aria-label={
              broken ? "Racha rota" : `Racha de ${streak} ${streak === 1 ? "día" : "días"}`
            }
            className={`hud-chamfer-sm flex items-center gap-1.5 px-3 py-2 ${
              broken ? "bg-surface-2 text-muted grayscale" : "bg-flame-soft text-flame"
            }`}
          >
            <span aria-hidden>🔥</span>
            <span className="font-display text-lg font-semibold">{streak}</span>
          </div>
          <div className="hud-chamfer-sm flex items-center gap-1.5 bg-gold-soft px-3 py-2">
            <span aria-hidden>🪙</span>
            <AnimatedNumber value={coins} className="font-display text-lg font-semibold text-gold" />
          </div>
        </div>
      </div>

      {broken && lostStreak >= 2 && (
        <p className="mt-2 text-xs text-muted">
          Racha de {lostStreak} días perdida. Hoy puede empezar otra.
        </p>
      )}

      <div className="mt-3">
        <ProgressBar pct={p.pct} color="var(--gold)" shine />
        <p className="mt-1.5 flex justify-between text-xs text-muted">
          <span>
            <AnimatedNumber value={p.current} /> / {p.needed} XP
          </span>
          <span>Faltan {Math.max(0, p.needed - p.current)} XP</span>
        </p>
      </div>
    </header>
  );
}
```

Nota: `hud-panel` se define en Task 7. Si se ejecuta Task 3 antes que Task 7, la clase simplemente no aplica estilo aún (no rompe). Mantener el orden del plan evita el hueco visual.

- [ ] **Step 2: Verificar en la app**

Run: `npm run dev` y abrir `http://localhost:3000`.
Expected: cabecera muestra `Nivel N` con el color del acento, el nombre de rango debajo (p. ej. "Guardián"), y las monedas/XP cuentan hacia arriba al cargar. Con reduce-motion activado en el navegador, aparecen ya en su valor final.

- [ ] **Step 3: Lint**

Run: `npm run lint`
Expected: sin errores.

- [ ] **Step 4: Commit**

```bash
git add src/components/dashboard/PlayerHeader.tsx
git commit -m "feat: cabecera como carne de aventurero (rango + contadores animados)"
```

---

### Task 4: Fila de tarea → subidón al completar

**Files:**
- Modify: `src/components/tasks/TaskRow.tsx`
- Modify: `src/app/globals.css` (keyframes `reward-float`, `row-sweep`, `check-pop` + reduce-motion)

**Interfaces:**
- Consumes: datos que la fila ya recibe (`xpReward`, `coinReward`, `streakBonus`, `completed`).
- Produces: sin cambios de contrato (`onToggle`/`onDelete` iguales). Solo feedback visual al **marcar** (no al desmarcar).

- [ ] **Step 1: Añadir keyframes en `globals.css`**

Insertar tras el bloque `.check-particle` (~línea 111, antes del bloque `.celebrate-toast`):

```css
/* Recompensa flotante (+XP/monedas) que sube y se desvanece al completar. */
@keyframes reward-float {
  0% { opacity: 0; transform: translateY(4px) scale(0.9); }
  20% { opacity: 1; transform: translateY(-2px) scale(1); }
  100% { opacity: 0; transform: translateY(-22px) scale(1); }
}
.reward-float {
  animation: reward-float 0.9s ease-out forwards;
}

/* Barrido dorado que recorre la fila al completar. */
@keyframes row-sweep {
  from { transform: translateX(-100%); opacity: 0.6; }
  to { transform: translateX(100%); opacity: 0; }
}
.row-sweep {
  animation: row-sweep 0.6s ease-out forwards;
}

/* Pop elástico del check al marcar. */
@keyframes check-pop {
  0% { transform: scale(1); }
  45% { transform: scale(1.28); }
  100% { transform: scale(1); }
}
.check-pop {
  animation: check-pop 0.4s cubic-bezier(0.2, 1.4, 0.5, 1);
}
```

- [ ] **Step 2: Añadir los nuevos selectores al bloque reduce-motion**

Actualizar el `@media (prefers-reduced-motion: reduce)`:

```css
@media (prefers-reduced-motion: reduce) {
  .xp-shine, .rise-in, .check-particle, .celebrate-toast, .levelup-badge,
  .num-bump, .reward-float, .row-sweep, .check-pop {
    animation: none;
  }
  .check-particle, .reward-float, .row-sweep { display: none; }
}
```

- [ ] **Step 3: Cablear los efectos en `TaskRow.tsx`**

Replace todo `src/components/tasks/TaskRow.tsx` con:

```tsx
"use client";

import { useState } from "react";
import { DIFFICULTY_LABELS, type Difficulty } from "@/lib/gamification";

// Direcciones fijas de las partículas del fogonazo al marcar (px).
const BURST = [
  [-14, -10], [14, -10], [-16, 6], [16, 6], [0, -18], [0, 16],
] as const;

export interface TaskItemData {
  id: string;
  title: string;
  difficulty: Difficulty;
  xpReward: number;
  coinReward: number;
  completed: boolean;
  goalTitle: string | null;
  dueDay: number | null;
  streakBonus: number; // monedas extra si se completa ahora; 0 sin racha
}

export function TaskRow({
  task,
  onToggle,
  onDelete,
}: {
  task: TaskItemData;
  onToggle: (id: string) => void;
  onDelete?: (id: string) => void;
}) {
  // Cambia en cada compleción para relanzar las animaciones (partículas,
  // pop del check, flotante de recompensa y barrido de la fila).
  const [burst, setBurst] = useState(0);
  const coinGain = task.coinReward + task.streakBonus;

  return (
    <div className="relative flex items-center gap-1">
      {/* Barrido dorado sobre toda la fila al marcar. */}
      {burst > 0 && (
        <span
          key={`sweep-${burst}`}
          aria-hidden
          className="row-sweep pointer-events-none absolute inset-y-0 left-0 w-1/3 bg-gradient-to-r from-transparent via-gold-soft to-transparent"
        />
      )}
      {/* Toda la fila es pulsable; el check queda a la derecha,
          en la zona natural del pulgar. */}
      <button
        onClick={() => {
          if (!task.completed) setBurst(Date.now());
          onToggle(task.id);
        }}
        className="flex min-h-14 w-full min-w-0 flex-1 items-center gap-3 py-2 text-left transition-opacity active:opacity-60"
      >
        <div className="min-w-0 flex-1">
          <p
            className={`text-sm font-medium transition-colors ${
              task.completed ? "text-muted line-through" : ""
            }`}
          >
            {task.title}
          </p>
          <p className="mt-0.5 text-xs text-muted">
            {task.goalTitle ? `${task.goalTitle} · ` : ""}
            {DIFFICULTY_LABELS[task.difficulty]} · +{task.xpReward} XP · +
            {task.coinReward} 🪙
            {!task.completed && task.streakBonus > 0 && (
              <span className="text-flame"> · 🔥 +{task.streakBonus}</span>
            )}
          </p>
        </div>
        <span
          aria-hidden
          className={`hud-chamfer-sm relative flex h-11 w-11 shrink-0 items-center justify-center border text-lg transition-colors ${
            task.completed
              ? "border-green bg-green-soft text-green"
              : "border-edge bg-surface-2 text-transparent"
          } ${burst > 0 ? "check-pop" : ""}`}
        >
          ✓
          {burst > 0 && (
            <span key={burst} className="pointer-events-none absolute inset-0">
              {BURST.map(([dx, dy], i) => (
                <span
                  key={i}
                  className="check-particle absolute left-1/2 top-1/2 h-1.5 w-1.5 rounded-full bg-green"
                  style={{ "--dx": `${dx}px`, "--dy": `${dy}px` } as React.CSSProperties}
                />
              ))}
            </span>
          )}
          {/* Recompensa flotante que sube desde el check. */}
          {burst > 0 && (
            <span
              key={`reward-${burst}`}
              aria-hidden
              className="reward-float pointer-events-none absolute -top-1 left-1/2 -translate-x-1/2 whitespace-nowrap font-display text-xs font-bold text-gold"
            >
              +{task.xpReward} XP · +{coinGain} 🪙
            </span>
          )}
        </span>
        <span className="sr-only">
          {task.completed ? "Desmarcar" : "Completar"} {task.title}
        </span>
      </button>
      {onDelete && (
        <button
          onClick={() => onDelete(task.id)}
          aria-label={`Eliminar ${task.title}`}
          className="flex h-11 w-8 shrink-0 items-center justify-center text-muted transition-colors hover:text-red active:text-red"
        >
          ✕
        </button>
      )}
    </div>
  );
}
```

- [ ] **Step 4: Verificar en la app**

Run: `npm run dev`, abrir `http://localhost:3000`, marcar una tarea.
Expected: el check hace pop, salen partículas, un barrido dorado cruza la fila y sube un flotante `+X XP · +Y 🪙`. Al **desmarcar** no aparece ningún efecto. Con reduce-motion, marcar cambia el estado sin flotante ni barrido.

- [ ] **Step 5: Lint**

Run: `npm run lint`
Expected: sin errores.

- [ ] **Step 6: Commit**

```bash
git add src/components/tasks/TaskRow.tsx src/app/globals.css
git commit -m "feat: subidon al completar tarea (pop, barrido y recompensa flotante)"
```

---

### Task 5: Overlay de subida de nivel revela el rango

**Files:**
- Modify: `src/components/celebration/CelebrationProvider.tsx` (solo el bloque del overlay `levelUp`)

**Interfaces:**
- Consumes: `rankFor` (Task 1). El `levelUp` ya es el número de nivel nuevo.
- Produces: sin cambios de API (`celebrate(result)` igual).

- [ ] **Step 1: Importar `rankFor`**

En `src/components/celebration/CelebrationProvider.tsx`, añadir a los imports (junto a la línea 5):

```tsx
import { rankFor } from "@/lib/gamification";
```

- [ ] **Step 2: Mostrar el nombre de rango en el overlay**

Replace el bloque `{levelUp !== null && ( ... )}` (líneas ~71-86) por:

```tsx
      {levelUp !== null && (
        <button
          type="button"
          onClick={() => setLevelUp(null)}
          aria-label="Cerrar aviso de subida de nivel"
          className="fixed inset-0 z-50 flex flex-col items-center justify-center gap-3 bg-bg/85 backdrop-blur-sm"
        >
          <p className="font-display text-sm uppercase tracking-[0.3em] text-muted">
            Subes de nivel
          </p>
          <div className="levelup-badge hud-chamfer flex h-32 w-32 items-center justify-center border-2 border-gold bg-gold-soft">
            <span className="font-display text-6xl font-bold text-gold">{levelUp}</span>
          </div>
          <p className="font-display text-lg font-semibold uppercase tracking-[0.18em] text-gold">
            {rankFor(levelUp).name}
          </p>
          <p className="text-xs text-muted">Toca para continuar</p>
        </button>
      )}
```

- [ ] **Step 3: Lint**

Run: `npm run lint`
Expected: sin errores.

- [ ] **Step 4: Commit**

```bash
git add src/components/celebration/CelebrationProvider.tsx
git commit -m "feat: el overlay de subida de nivel revela el rango alcanzado"
```

---

### Task 6: Progreso semanal → anillo radial hero

**Files:**
- Create: `src/components/ui/RadialProgress.tsx`
- Modify: `src/app/globals.css` (clase `.radial-ring` + entrada reduce-motion)
- Modify: `src/components/dashboard/WeekProgress.tsx` (solo la cabecera de la tarjeta)

**Interfaces:**
- Consumes: `<AnimatedNumber>` (Task 2).
- Produces: `<RadialProgress pct={number} size?={number} stroke?={number} color?={string}>{children}</RadialProgress>` — componente de servidor (SVG estático con transición en cambios de valor).

- [ ] **Step 1: Crear `RadialProgress`**

Create `src/components/ui/RadialProgress.tsx`:

```tsx
// Anillo de progreso HUD (SVG). El contenido central (children) se superpone.
// La transición del trazo se anula bajo prefers-reduced-motion vía .radial-ring.
export function RadialProgress({
  pct,
  size = 88,
  stroke = 8,
  color = "var(--violet)",
  children,
}: {
  pct: number;
  size?: number;
  stroke?: number;
  color?: string;
  children?: React.ReactNode;
}) {
  const clamped = Math.max(0, Math.min(100, pct));
  const r = (size - stroke) / 2;
  const circ = 2 * Math.PI * r;
  const offset = circ * (1 - clamped / 100);

  return (
    <div className="relative shrink-0" style={{ width: size, height: size }}>
      <svg width={size} height={size} className="-rotate-90">
        <circle
          cx={size / 2}
          cy={size / 2}
          r={r}
          fill="none"
          stroke="var(--surface-2)"
          strokeWidth={stroke}
        />
        <circle
          cx={size / 2}
          cy={size / 2}
          r={r}
          fill="none"
          stroke={color}
          strokeWidth={stroke}
          strokeLinecap="round"
          strokeDasharray={circ}
          strokeDashoffset={offset}
          className="radial-ring"
        />
      </svg>
      <div className="absolute inset-0 flex flex-col items-center justify-center">
        {children}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Añadir `.radial-ring` en `globals.css`**

Insertar tras el bloque `.num-bump` (creado en Task 2):

```css
/* Trazo del anillo de progreso: anima al cambiar el porcentaje. */
.radial-ring {
  transition: stroke-dashoffset 0.7s ease-out;
}
```

Y en el bloque `@media (prefers-reduced-motion: reduce)`, añadir tras la llave de cierre de la regla `animation: none;` (dentro del media query):

```css
  .radial-ring { transition: none; }
```

- [ ] **Step 3: Usar el anillo en `WeekProgress.tsx`**

En `src/components/dashboard/WeekProgress.tsx`, actualizar imports (líneas 1-2) a:

```tsx
import { Card, SectionTitle } from "@/components/ui/Card";
import { RadialProgress } from "@/components/ui/RadialProgress";
import { AnimatedNumber } from "@/components/ui/AnimatedNumber";
```

(Se elimina el import de `ProgressBar`, que ya no se usa en esta tarjeta.)

Replace el bloque `<div className="mt-3 flex items-center gap-3"> ... </div>` (líneas ~35-43) por:

```tsx
      <div className="mt-3 flex items-center gap-4">
        <RadialProgress pct={donePct}>
          <span className="font-display text-2xl font-bold text-violet">
            <AnimatedNumber value={donePct} />%
          </span>
        </RadialProgress>
        <div className="flex-1">
          <p className="font-display text-sm font-semibold">
            {doneCount} de {totalCount} tareas
          </p>
          <p className="mt-1 text-xs text-muted">
            {donePct === 100 && totalCount > 0
              ? "¡Semana redonda!"
              : "Completadas esta semana"}
          </p>
        </div>
      </div>
```

- [ ] **Step 4: Verificar en la app**

Run: `npm run dev`, abrir `http://localhost:3000`.
Expected: la tarjeta "Esta semana" muestra un anillo con el `%` animado en el centro. La lista de objetivos debajo se mantiene igual. Con reduce-motion, el número aparece final y el anillo no transiciona.

- [ ] **Step 5: Lint**

Run: `npm run lint`
Expected: sin errores (verificar que no queda import sin usar de `ProgressBar`).

- [ ] **Step 6: Commit**

```bash
git add src/components/ui/RadialProgress.tsx src/components/dashboard/WeekProgress.tsx src/app/globals.css
git commit -m "feat: progreso semanal con anillo radial animado"
```

---

### Task 7: Capa premium global (paneles, nav)

**Files:**
- Modify: `src/app/globals.css` (utilidad `.hud-panel`)
- Modify: `src/components/ui/Card.tsx` (aplicar `hud-panel`)
- Modify: `src/components/layout/BottomNav.tsx` (realce del estado activo)

**Interfaces:**
- Consumes: nada nuevo.
- Produces: clase `.hud-panel` reutilizable (gradiente sutil + borde con glow interior). `PlayerHeader` (Task 3) ya la referencia.

- [ ] **Step 1: Definir `.hud-panel` en `globals.css`**

Insertar tras el bloque `.hud-chamfer-sm` (~línea 85):

```css
/* Panel HUD premium: gradiente sutil sobre la superficie + glow de borde
   interior. Da profundidad sin introducir colores fuera de los tokens. */
.hud-panel {
  background-image: linear-gradient(
    160deg,
    color-mix(in srgb, var(--surface-2) 55%, transparent),
    var(--surface) 60%
  );
  box-shadow: inset 0 1px 0 0 color-mix(in srgb, var(--violet) 12%, transparent);
}
```

- [ ] **Step 2: Aplicar `hud-panel` en `Card`**

En `src/components/ui/Card.tsx`, cambiar la línea del `<section>` (línea 9):

```tsx
    <section className={`hud-panel rounded-lg border border-edge p-4 ${className}`}>
```

(Se retira `bg-surface`: el gradiente de `hud-panel` cubre el fondo.)

- [ ] **Step 3: Realzar el estado activo del nav**

En `src/components/layout/BottomNav.tsx`, replace el bloque `<Link ...>` completo (líneas 24-39) por:

```tsx
            <Link
              key={tab.href}
              href={tab.href}
              aria-current={active ? "page" : undefined}
              className={`relative flex min-h-14 flex-1 flex-col items-center justify-center gap-0.5 text-[11px] transition-colors ${
                active ? "text-gold" : "text-muted hover:text-ink"
              }`}
            >
              {active && (
                <span
                  aria-hidden
                  className="absolute inset-x-4 top-0 h-0.5 rounded-full bg-gold"
                  style={{ boxShadow: "0 0 8px var(--gold)" }}
                />
              )}
              <span
                className="text-lg leading-none"
                aria-hidden
                style={active ? { filter: "drop-shadow(0 0 6px var(--gold-soft))" } : undefined}
              >
                {tab.icon}
              </span>
              <span className={`font-display ${active ? "font-semibold" : ""}`}>
                {tab.label}
              </span>
            </Link>
```

- [ ] **Step 4: Verificar en la app**

Run: `npm run dev`, recorrer las pestañas.
Expected: tarjetas con profundidad sutil (gradiente + línea de brillo superior); la pestaña activa del nav tiene una barra dorada con glow arriba y el icono resaltado. Nada se desplaza ni pierde zona táctil.

- [ ] **Step 5: Lint**

Run: `npm run lint`
Expected: sin errores.

- [ ] **Step 6: Commit**

```bash
git add src/app/globals.css src/components/ui/Card.tsx src/components/layout/BottomNav.tsx
git commit -m "feat: capa premium (paneles con profundidad y nav activo con glow)"
```

---

### Task 8: Vitrina de trofeos elevada

**Files:**
- Create: `src/components/goals/TrophyCard.tsx`
- Modify: `src/app/goals/page.tsx` (bloque `🏆 Vitrina`, ~líneas 229-250)

**Interfaces:**
- Consumes: `goalXpFrom`, `levelForXp` (ya importados en `goals/page.tsx`).
- Produces: `<TrophyCard title icon level month />` — presentación de un trofeo.

- [ ] **Step 1: Crear `TrophyCard`**

Create `src/components/goals/TrophyCard.tsx`:

```tsx
// Trofeo de la vitrina: objetivo LP conseguido. Peso visual (acento dorado,
// nivel destacado) para que dé orgullo mirarlo. Solo presentación.
export function TrophyCard({
  title,
  icon,
  level,
  month,
}: {
  title: string;
  icon: string | null;
  level: number;
  month: string;
}) {
  return (
    <div className="hud-chamfer flex items-center gap-3 border border-gold/40 bg-gold-soft p-3">
      <div className="hud-chamfer-sm flex h-12 w-12 shrink-0 items-center justify-center bg-surface text-2xl">
        {icon || "🏆"}
      </div>
      <div className="min-w-0 flex-1">
        <p className="truncate text-sm font-semibold">{title}</p>
        <p className="mt-0.5 text-xs text-muted">Conseguido · {month}</p>
      </div>
      <span className="hud-chamfer-sm shrink-0 bg-surface px-2.5 py-1 font-display text-sm font-bold text-gold">
        Nv. {level}
      </span>
    </div>
  );
}
```

- [ ] **Step 2: Usar `TrophyCard` en la vitrina**

En `src/app/goals/page.tsx`, añadir el import (junto a los demás imports de componentes):

```tsx
import { TrophyCard } from "@/components/goals/TrophyCard";
```

Replace el bloque `{trophies.length > 0 && ( ... )}` (líneas ~229-250) por:

```tsx
      {trophies.length > 0 && (
        <section className="space-y-3">
          <SectionTitle>🏆 Vitrina</SectionTitle>
          <div className="space-y-2">
            {trophies.map((g) => (
              <TrophyCard
                key={g.id}
                title={g.title}
                icon={g.icon}
                level={levelForXp(goalXpFrom(g.weeklyGoals))}
                month={
                  g.completedAt?.toLocaleDateString("es-ES", {
                    month: "long",
                    year: "numeric",
                  }) ?? ""
                }
              />
            ))}
          </div>
        </section>
      )}
```

Nota: si tras esto `Card` o `levelForXp`/`goalXpFrom` quedaran sin usar en `goals/page.tsx`, dejarlos si siguen usándose en otras secciones de la página (lo están: `Card` en la vitrina se sustituye, pero `levelForXp`/`goalXpFrom` se usan arriba en la lista de objetivos, línea ~75/238). Verificar con lint en el paso siguiente.

- [ ] **Step 3: Verificar en la app y lint**

Run: `npm run dev`, abrir `http://localhost:3000/goals` (con al menos un objetivo conseguido; si no hay, marcar uno como "Conseguido").
Expected: la vitrina muestra tarjetas de trofeo con acento dorado, icono en recuadro, título, mes y el nivel destacado.

Run: `npm run lint`
Expected: sin errores ni imports sin usar.

- [ ] **Step 4: Commit**

```bash
git add src/components/goals/TrophyCard.tsx src/app/goals/page.tsx
git commit -m "feat: vitrina de trofeos elevada (TrophyCard con acento dorado)"
```

---

### Task 9: Verificación integral y build

**Files:** ninguno (solo verificación).

- [ ] **Step 1: Suite de dominio**

Run: `npm test`
Expected: todo verde (incluye `rankFor`).

- [ ] **Step 2: Lint global**

Run: `npm run lint`
Expected: sin errores.

- [ ] **Step 3: Build de producción**

Run: `npm run build`
Expected: build `standalone` sin errores de tipos ni de RSC/cliente.

- [ ] **Step 4: Recorrido en app real (skill verify)**

Seguir `.claude/skills/verify/SKILL.md` (contenedor limpio + `scripts/e2e-drive.mjs`). Comprobar manualmente los momentos clave:
- Cabecera: rango + contadores animados.
- Marcar tarea: pop, partículas, barrido, recompensa flotante.
- Subida de nivel: overlay revela el rango.
- Progreso semanal: anillo radial.
- Nav: pestaña activa con glow. Vitrina: trofeos dorados.
- Activar `prefers-reduced-motion` en el navegador y confirmar que todo degrada a estado final sin movimiento.

- [ ] **Step 5: Actualizar AGENTS.md si procede**

Si algún concepto de UI nuevo merece documentarse (p. ej. rangos por nivel, utilidad `.hud-panel`), añadir una línea breve en la sección "Convenciones" de `AGENTS.md`. Commit aparte:

```bash
git add AGENTS.md
git commit -m "docs: rangos por nivel y utilidad hud-panel en convenciones"
```

---

## Notas de ejecución

- Ejecutar las tareas **en orden**: Task 2 define `AnimatedNumber` y `num-bump` que consumen 3 y 6; Task 7 define `.hud-panel` que referencia la cabecera de Task 3 (referencia inocua si aún no existe, pero el orden evita el hueco visual).
- Cada tarea que modifica `globals.css` amplía el **mismo** bloque `@media (prefers-reduced-motion: reduce)`. Al ejecutarlas por separado, releer el bloque actual antes de editar para no pisar entradas previas.
